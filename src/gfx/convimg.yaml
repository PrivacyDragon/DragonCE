palettes:
  - name: global_palette
    images: automatic

converts:
  - name: tileset
    palette: global_palette
    compress: zx7
    tilesets:
      tile-width: 80
      tile-height: 80
      images:
        - /home/bas_van_h/CEdev/projects/Draak/src/gfx/image.png

outputs:
  - type: appvar
    name: DRAGAN
    source-format: c
    include-file: DRAGAN.h
    header-string: STRING
    palettes:
      - global_palette
    converts:
      - tileset

